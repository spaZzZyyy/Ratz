using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class testSpears : MonoBehaviour
{
  /*  [SerializeField] public bool turnOn;
    [SerializeField] private float timer;
    bool fall1 = false;
    bool fall2 = false;
    bool fall3 = false;

    // Update is called once per frame
    void Update()
    {
        if (turnOn)
        {
            timer = 4f;
            turnOn = false;
            fall1 = true;
            fall2 = true;
            fall3 = true;
        }

        timer -= Time.deltaTime;

        if (timer < 3f && fall1)
        {
            fall1 = false;
            transform.GetChild(0).gameObject.GetComponent<ShootSpear>().boolShoot = true;
        }
        if (timer < 2.75f && fall2)
        {
            fall2 = false;
            transform.GetChild(1).gameObject.GetComponent<ShootSpear>().boolShoot = true;
        }
        // if (timer < 2.5f && fall3)
        // {
        //     fall3 = false;
        //     transform.GetChild(2).gameObject.GetComponent<ShootSpear>().boolShoot = true;
        // }
    }*/
}
